# Cloud Ransomware Kill-Switch & IAM Permission-Toxicity Detector
# Python 3.11+

# AWS SDK
boto3>=1.34.0
botocore>=1.34.0

# Neo4j graph database driver (Phase 3)
neo4j>=5.18.0

# Environment variable management
python-dotenv>=1.0.0

# CLI output formatting
rich>=13.7.0



# Statistical utilities (used in EWMA calculations)
# Note: stdlib math module is used for sqrt; this is for any future extensions
# numpy is intentionally excluded — we implement EWMA from scratch to keep
# Lambda package size small and avoid numpy layer dependency.
